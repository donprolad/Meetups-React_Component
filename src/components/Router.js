import React from "react";
import { BrowserRouter, Switch, Route } from "react-router-dom";

const Router = any => {
  <BrowserRouter>
    <Route exact path="/categories" component={any} />
  </BrowserRouter>;
};

export default Router;
